@*@{
    ViewBag.Title = "GioHang";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using WebLacome.Models;
@model List<GioHang>
<h2 style="text-align:center">Giỏ Hàng Của Bạn</h2>
<div class="card border-dark">
    <table class="table">
        <tr style="background-color:lightyellow">
            <th>Mã Mỹ Phẩm</th>
            <th>Tên Mỹ Phẩm</th>
            <th>Hình Ảnh</th>
            <th>Số Lượng</th>
            <th>Đơn Giá</th>
            <th>Thành Tiền</th>
            <th><a href="@Url.Action("XoaGioHang_All","GioHang")">Delete All</a></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td width="10%">@item.iMAMP</td>
                <td width="20%">@item.iTENMP</td>
                <td width="20%"><img src="~/Content/HinhAnhSP/@item.iANH" width="50%"></td>
                <td width="20%">
                    <form action="@Url.Action("CapNhatGioHang","GioHang", new {@MAMP = item.iMAMP})" method="post">
                        <input value="@item.iSOLUONG" type="number" name="soluong" min="1" max="100" />
                        <button class="btn btn-success updatecartitem" type="submit" data-productid="@item.iMAMP">
                            Update
                        </button>
                    </form>
                </td>
                <td width="15%">@string.Format("{0:0,0}", item.iGIA) </td>
                <td width="15%">@string.Format("{0:0,0}", item.ThanhTien)</td>
                <td>
                    <a href="@Url.Action("Details","MyPham",new {@MAMP = item.iMAMP})">Details</a>
                    <a href="@Url.Action("XoaGioHang","GioHang", new {@MAMP = item.iMAMP})">Delete</a>
                </td>
            </tr>
        }
        <tr style="background-color:lightyellow">
            <td colspan="3" style="color:blue;font-weight:bold;text-align:right;">Tổng Cộng</td>
            <td style="color:red;font-weight:bold;">@ViewBag.TongSoLuong</td>
            <td> </td>
            <td style="color:red; font-weight:bold;">@string.Format("{0:0,0}", ViewBag.TongThanhTien) VNĐ</td>
        </tr>
    </table>
</div>*@


